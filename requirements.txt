# ============================================================
# findWaterError - 依赖包清单
# ============================================================
# 环境：Python 3.13.5 | CUDA 13.1 | PyTorch 2.6.0+cu124
# GPU: NVIDIA GeForce RTX 3060 (12GB)
# 创建时间：2026-04-03
# ============================================================

# ============================================================
# 核心深度学习框架
# ============================================================
# PyTorch 2.6.0 (CUDA 12.4 编译版本，兼容 CUDA 13.1)
torch==2.6.0+cu124
torchvision==0.21.0+cu124
torchaudio==2.6.0+cu124

# ============================================================
# Transformers 和预训练模型
# ============================================================
transformers==4.48.0  # DINOv2 需要
huggingface-hub>=0.20.0
tokenizers>=0.15.0
safetensors>=0.4.0

# ============================================================
# 数据处理和可视化
# ============================================================
numpy>=2.0.0
pandas>=2.2.0
pillow>=11.0.0
matplotlib>=3.10.0
seaborn>=0.13.0
scikit-learn>=1.6.0
scikit-image>=0.25.0
opencv-python>=4.9.0  # 可选，用于图像预处理

# ============================================================
# YOLO 系列（YOLOv8, YOLOv10, YOLO-SE）
# ============================================================
ultralytics>=8.3.0  # YOLOv8
# YOLOv10 使用 ultralytics 或从源码安装
# YOLO-SE 可能需要自定义安装

# ============================================================
# 工具库
# ============================================================
tqdm>=4.66.0  # 进度条
pyyaml>=6.0  # YAML 配置
tensorboard>=2.15.0  # 训练可视化
wandb>=0.16.0  # 可选，实验追踪

# ============================================================
# Jupyter 和相关工具
# ============================================================
jupyter>=1.0.0
jupyterlab>=4.0.0
notebook>=7.0.0
ipykernel>=6.0.0

# ============================================================
# 开发和测试
# ============================================================
pytest>=8.0.0
pytest-cov>=4.0.0
black>=24.0.0  # 代码格式化
flake8>=7.0.0  # 代码检查

# ============================================================
# 系统依赖（Windows）
# ============================================================
# 注意：以下包在 Windows 上可能需要特殊安装
# 建议使用 conda 或预编译的 wheel 文件

# ============================================================
# 安装说明
# ============================================================
# 1. 基础安装（仅 CPU）:
#    pip install -r requirements.txt
#
# 2. GPU 加速安装（推荐）:
#    pip install -r requirements.txt
#
# 3. 如果 PyTorch 安装失败，使用官方源：
#    pip install torch==2.6.0 torchvision==0.21.0 torchaudio==2.6.0 --index-url https://download.pytorch.org/whl/cu124
#
# 4. 国内镜像源（可选）:
#    pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
# ============================================================
